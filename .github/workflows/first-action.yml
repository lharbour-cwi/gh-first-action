name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello World!"
            echo "It's Alive!"
      - name: Print goodbye
        run: |
            echo "I'm cooked!"
            echo "Gobble Gobble"
